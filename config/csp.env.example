# CSP launch configuration (sourceable shell file)

# Gateway
# CSP_PORT=8765
# CSP_AUTH_TOKEN=
# CSP_GATEWAY_URL=http://localhost:8765

# Orchestrator
# CSP_ORCHESTRATOR=0
# CSP_ORCH_CMD="claude --model haiku --dangerously-skip-permissions"

# Agent commands (override if CLIs are not in PATH)
# CSP_CLAUDE_CMD="claude --dangerously-skip-permissions"
# CSP_GEMINI_CMD="gemini"
# CSP_CODEX_CMD="codex"

# Preflight checks (set to 1 to fail if CLI missing)
# CSP_STRICT_CLI_CHECK=0

# Config file override (optional)
# CSP_CONFIG_FILE=/path/to/csp.env
